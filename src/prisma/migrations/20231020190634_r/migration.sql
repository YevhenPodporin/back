/*
  Warnings:

  - You are about to drop the column `last_message` on the `notifications` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE `notifications` DROP COLUMN `last_message`;
